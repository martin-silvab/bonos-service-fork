apiVersion: v1
kind: Service
metadata:
  name: bonos-service
  labels:
    app: bonos-service
spec:
  type: ClusterIP
  selector:
    app: bonos-service
  ports:
    - name: http
      protocol: TCP
      port: 8004
      targetPort: 8004